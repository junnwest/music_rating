# Pipeline health checks

Periodic health checks for the MusicBrainz catalog pipeline.

> **If you are a Claude Code session, do this at session start:** read **Schedule** + the
> top row of **Check log** below. If today's date ≥ `next due` **and** the pipeline is
> running, then RUN the commands in **How to run**, interpret per **Reading the results**,
> report a short summary to the user, and **prepend a new dated row to the Check log** with
> the next due date. If it's not due yet, do nothing (don't nag). Always run the checks
> (regardless of due date) right after a pipeline restart, a migration, or a pipeline code
> change. Convert any relative dates to absolute. Don't auto-fix data — report and propose.

## Schedule (cadence)
- **Daily** while the launch 5k is draining — until `pending` ≈ 0 (est. **~2026-07-03 to 2026-07-08**).
- **Weekly** afterward (steady state: FRESHNESS re-polls + slow DISCOVER growth).
- **Always** after: a pipeline restart · a migration applied · a pipeline code change · the machine was off for hours.

## How to run (from `apps/web/`)
```
npm run pipeline:status   # liveness, throughput, ETA  (daily)
npm run pipeline:verify   # 7-check structural battery  (daily)
npm run mb:audit          # deeper data view: sample eyeball, skipped list, source purity, ISRC  (every ~3 days, or if verify flags)
```

## Reading the results — what's fine vs a real problem
**GOOD (no action):**
- `pipeline:verify` → **7/7, "structurally clean"**. The `N empty artist(s) to review` note is **structurally** harmless (0 release_groups = no bad data, nothing to corrupt) — but see the warning below before reading it as "nothing to do".

> ⚠️ **The `N empty artist(s)` note is no longer a small benign footnote (revised 2026-07-28).**
> The original text here — "features-only acts (e.g. SEHUN) plus a few known generic-name
> false-matches (kai/dean/gray class)" — was written when N was a handful. It predates the
> **`area` discovery lane (2026-07-17)**, which queues every MB `country:KR` / `area:<city>`
> **artist entity**; MB entities exist without any releases, so N is now **8,973 (13.3% of the
> catalog; 7,662 KR = 61.7% of all KR artists)**. Sampled against the live MB API, ~98% have zero
> release-groups **in MusicBrainz itself** — so it is a coverage gap, not an ingest failure, and
> `pipeline:verify` passing is correct. Still **not** something to auto-fix: the rows are inert and
> the FRESHNESS lane keeps re-polling them, so they self-heal when MB catalogs a release. They are
> hidden from search as of migration `20260728000000`. Only treat a **rising** N as news.
- `pipeline:status` throughput: healthy is **≳ 25 artists/hr**. A reading taken right after a restart is **understated** (downtime sits in the 60-min window) — re-check before reacting.
- The `qc` **lane** heartbeat in `pipeline:status` can lag the live truth (it re-runs hourly and reflects the running process's code). **Trust `pipeline:verify` over a stale qc heartbeat.**
- Two lanes added 2026-07-15 (iTunes recency bridge) are **expected** in `pipeline:status`: **`recency`** (auto-ingests recent KR releases MB lacks; `idle (swept)` = finished a full ~783-artist sweep, normal; `blocked (iTunes 403)` = shared GAPFILL cooldown, transient) and **`reconcile`** (daily; links `source='itunes'` rows to MB once it catches up). Both ON by default; `RECENCY=0` disables both. A slow but nonzero `recency` ingest rate is normal (5 artists/30m). If `reconcile` reports "merge-needed", that's a genuine cross-source dup to look at (rare).
- The **`area`** lane (added 2026-07-17) is **expected** in `pipeline:status`: it re-sweeps MB `country:KR` + city areas at most weekly to keep KR coverage current, queuing new `source='mbid'` artists. `idle (next sweep in ~Nh)` is the normal steady state — it only shows `running` for ~4m during its weekly sweep. `AREA_DISCOVERY=0` disables it. A one-off large `+N queued` after a sweep just means MB catalogued new KR artists; they drain via `ingest` like any queue rows.

**REAL problems → tell the user / investigate:**
- `pipeline:status` shows `last 60m 0/hr` while `ingest` says `running` → **stalled** (machine asleep? MB unreachable? supervisor stuck).
- `pipeline:verify` **structural** fail — duplicate artists, canonical (>1 / 0 canonical), orphan releases, or unexpected `source` (NOT the empty-artists note).
- `pending` not decreasing across a full day.
- `mb:audit` sample shows artists with a country that contradicts the seed region (e.g. a Korean name as `[US]`/`[AU]`) → false-match (the iTunes-shadow / generic-name class).
- Throughput holding **≤ 20/hr** for multiple days → 5k won't finish in the 2-week window; revisit the local-MB-mirror option.

## Check log (newest first)
| Date | verify | rate (last 60m) | pending | notes / next due |
|------|--------|-----------------|---------|------------------|
| 2026-09-02 (data corruption found + fixed + Spotify circuit bug found + fixed) | not run (incident response took priority over a routine check) | n/a — same `--fresh-focus` setup | 0 | **🐛🚨 A monitor notification caught live data corruption: `pickAlbum()` in `itunes-client.ts`'s title-only fallback (zero artist check) let 28 releases from an unrelated iTunes catalog attach to the real artist "$NOT."** Stopped the pipeline within minutes, root-caused, fixed (added a loose artist-match check to the fallback), deleted the 28 bad rows (0 ratings existed on them), and audited the wider blast radius before trusting it was isolated — 0 further suspects across 64 other recent `[itunes]` release groups and 600 gapfill-checked groups this session (one caveat: 44 gapfill cover-only fills have no raw-artist trail left to audit retroactively). **🐛🚨 While restarting to ship that fix, found a second, more serious bug: the `recency-spotify` lane never checked the shared production Spotify circuit breaker before calling Spotify, and had no request spacing at all (unlike the iTunes client) — it burst through a batch with zero delay, tripped a real ~18.8-hour Spotify penalty, and kept calling anyway, actively degrading production web search for real users** (confirmed directly: `spotify:rate-limited-until` read back ~18.8h remaining). Fixed three ways: request spacing added to `spotify-client.ts`, the shared circuit checked before every Spotify call (new `spotifyCircuitRemainingMs()`), and `pipeline.ts` checking it again before claiming a batch (needed separately — the lane's cursor advances unconditionally per artist, so claiming a batch while blocked would silently skip-scan and permanently lose that slice of the sweep). **Verified live**: `recency-spotify` now correctly reports `blocked (shared circuit) ... done=0 «~1125m remaining»` — no cursor loss, no further Spotify calls. The 18.8h block itself is real Spotify-side state, not something we can clear — it just has to expire; the fix stops us from re-triggering or prolonging it. **`gapfill`/`recency`(iTunes) currently show `blocked (iTunes 403)` — unrelated, self-heals in 120m, a consequence of the heavy iTunes traffic from this incident's own investigation/verification, not a new bug.** `tsc --noEmit` clean throughout. **Next due: check `recency-spotify` resumes normally once the shared circuit clears (~2026-09-03 ~10:39 UTC); spot-check a handful of other artists with unusually short/generic titles for the same class of corruption if time allows; 2026-09-08 for the regular weekly check otherwise.** |
| 2026-09-02 (new `recency-spotify` lane added) | not run (feature work, not a routine check) | n/a — same `--fresh-focus` setup | 0 | **New lane: `recency-spotify`**, a second independent MB-lag bridge alongside the existing iTunes `recency` lane — built after checking 9 releases missing from both MB and iTunes and finding Spotify had 6 of them same-day. New `spotify-client.ts` (cooperates with the production-shared Spotify rate-limit circuit in `spotify-circuit.ts`), `spotify-ingest-core.ts` (dedicated writer, `source='spotify'`), `discover-spotify-recency.ts` (same dedup-safe scan as the iTunes version). Wired in with its own cursor file + circuit state so a block on one source can't stall the other. `reconcile-itunes-mb.ts` already handled `source='spotify'` — no changes needed there. **🐛 Found live at pipeline scale (not caught by CLI testing): `spotify-client.ts` had no fetch timeout, so the second real batch hung for 20+ minutes on a single stalled connection** (this lane has no watchdog, same gap as `recency`). Fixed with `AbortSignal.timeout(15000)` on every Spotify call; confirmed live immediately after restart — next batch found 13 real releases in one pass (BUNT. ×5, Phantogram, Vicente Fernández ×6, Hologram Lo'). Verified the write path directly against the DB before trusting the pipeline run (SUPERBEE `--ingest` test → confirmed release_group/release/recordings/release_tracks all correct, including the user's originally-requested "82 life flow"). `tsc --noEmit` clean. **Next due: watch `recency-spotify`'s hit rate over the next several cycles for the same kind of confidence already established for `recency`(iTunes); consider adding watchdog protection to both recency lanes if a hang ever recurs despite the timeout fix; 2026-09-08 for the regular weekly check otherwise.** |
| 2026-09-02 (qc disabled + recency cursor bug fixed + widened) | not run (real Supabase Disk IO Budget alert + a bigger live-data-collection bug took priority over a routine structural check) | n/a — same `--fresh-focus` setup | 0 | **Real external alert, not a routine check: Supabase emailed that project `bside` is depleting its Disk IO Budget.** Root-caused to `qc`'s crash-loop: `integrityCheck()` is meant to run once/hour but had been timing out *before* reaching that sleep, so the supervisor's 30s crash-restart made it retry the same expensive full-table scan roughly every 30-60s continuously since the 09-01 restart. **`qc` disabled (`--no-qc`)**, freshness/gapfill carried over cleanly across the restart. Supabase's actual IO-budget % isn't exposed via the Management API — this is "a real contributor," not confirmed as "the" cause. **🐛 Much bigger find, prompted by the user asking "are we actually collecting new releases": the `recency` (iTunes) lane's cursor query has been silently broken since the first time it ever completed a full sweep.** `.gt('id', after)` with `after=''` throws `22P02 invalid uuid syntax` on the `uuid` id column; the code never checked `error`, so the failed query was silently read as "swept the whole set" — and since that branch re-saves the cursor as `''` again, every attempt after the first wrap hit the identical error forever, permanently stuck reporting "0 ingested." This was already true before this session touched anything (the empty-cursor state is reached at the end of every normal sweep) — likely a long-running silent no-op, not a regression from today. **Fixed**: only apply `.gt()` when the cursor is non-empty, check `error` explicitly. **Live-confirmed within minutes**: first real batch post-fix caught genuine MB-missing releases for Matheus & Kauan (Brazilian, +8), DJ CHARI (Japanese, +5), miwa (Japanese, +3), Donovan Woods (Canadian, +2 so far) — all non-Korean, all proof this lane is doing real work again after however long it wasn't. **Also widened `recency` from KR-only to catalog-wide** (dropped `.eq('country', RECENCY_COUNTRY)`, pass each artist's own country as the search-language hint instead of one fixed value) — the KR-only scope was separately excluding non-KR artists entirely (Fat Joe, Lil Wayne) and silently dropping null-country Korean artists (SUPERBEE, Rosy Pereira) even within its intended scope. Batch/interval bumped 5-per-30min → 25-per-10min so the ~5.6x larger population still completes a full sweep in ~19 days. `tsc --noEmit` clean both changes. **Known gap, not yet fixed: `recency` has no watchdog** (unlike `ingest`/`freshness`) — a genuinely hung iTunes fetch would have nothing to force a restart; not observed as a real problem yet (first batch was just slow, ~10min/25 artists, kept producing output throughout). **Next due: watch the recency hit rate hold up over the next several cycles; re-check whether the Disk IO alert recurs; 2026-09-08 for the regular weekly check otherwise.** |
| 2026-09-01 (restart after ~11d down) | not completed (stalled after `migrations` under live write load — the documented verify-vs-ingest contention; used `pipeline:status` + `qc` as the trusted live signal instead, per precedent) | n/a — `--fresh-focus`, queue empty by design; judge by the `freshness` lane | 0 | **First restart with BOTH freshness fixes live together** (the heavily-featured-artist park + the claim-on-read fix from this session). Last real ingest before this was 2026-08-21 — **~11 days down**, not a crash, just left stopped. **Healthy on restart:** `freshness` running, `done=17 err=0` and climbing within the first 40 min, including a genuine new find (`Mac Miller +1 new group`) — confirms it's doing real work, not just idling. `gapfill` made small steady real progress (+2, then +5, then +4 covers). `newreleases`' first sweep came back clean (`+1 flagged`) — expected to read low right after downtime, same as every prior restart, since the lane only moves `next_check_at` backward and most of the catalog was already overdue. Queue: still fully drained (`pending 0`), `failed` still **8, unchanged** for weeks. **Neither fix got directly exercised yet this restart** (no heavily-featured artist reached the head of a tier in the first 40 min, and no watchdog/crash interruption occurred to test the claim) — both were already verified live in isolation in the 2026-08-18/08-19 sessions, so this is a "still fine, not yet re-proven under combination" note rather than a concern. **🐛 New-ish finding: the `qc` lane is crash-looping** — 5 `canceling statement due to statement timeout` failures in the first ~40 minutes, each self-healing via the supervisor's 30s auto-restart, never blocking any other lane. Doesn't look like a regression from anything touched this session (qc's query shape is unchanged) — more likely the catalog has simply grown past whatever it could reliably query in time (`release_tracks` is now 4.6M rows, same table `tiering` already struggles against). **Not auto-fixed, proposing only:** either raise the statement timeout for that specific query or split/optimize it; worth a look next session if the crash rate doesn't taper off on its own. **Next due: 2026-09-08** (weekly), or immediately if the qc crash-loop worsens or a heavily-featured park/claim-interruption is worth confirming directly. |
| 2026-08-19 (claim-fix restart) | 6/7 (`integrity` only) | n/a — queue empty by design under `--fresh-focus`; judge by the `freshness` lane | 0 | **Verification of the claim-on-read fix (second livelock). ✅ WORKS.** Ella Fitzgerald (670 RGs) had been re-claimed every ~11 min since ~06:40 — the **ingest watchdog** killing her re-poll mid DB-write phase ("no MB activity 603s"), which is not an exception, so the previous catch-based fix never fired and `next_check_at` stayed at 2026-07-05. After the restart her `next_check_at` was stamped **2026-08-20T09:09** at the claim, the hot-tier head moved on to Mal Waldron, and hot-tier due count fell 2,086 → 1,303. At the time of this check her `last_ingested_at` was still 2026-07-04, and this row originally concluded that "any artist in roughly the 250–800 RG band will fail its re-poll once per day, forever". **⚠ THAT CONCLUSION WAS WRONG — corrected 2026-08-21.** All three exemplars have since re-polled **successfully**: **Ella Fitzgerald (670 RGs) at 2026-08-21T10:13**, Dolly Parton (375) at 11:04, Erik Satie (267) at 2026-08-20T22:20 — each with a clean `next_check_at = last_ingested_at + 1 day`. The watchdog kills were **environmental, not size-determined**: they happened while MusicBrainz was 503-throttling and the Supabase client was wedging, which stretched a normal re-poll past the 600s no-MB-activity window. A re-poll of an already-ingested artist is mostly idempotent upserts and is far cheaper than a cold ingest. **So there is no permanently-unrefreshable size band, and no watchdog change is needed.** The claim fix remains correct and necessary — it is what stops an interrupted re-poll (for any reason) from being re-claimed forever — but its value is robustness, not rescuing a doomed cohort. **⚠ CORRECTION to the two rows below: the "groups with 0 canonical" climb (1 → 2 → 4 → 6) is NOT a trend — it is measurement noise.** This session it read **27** in `qc` and **10** in `verify` six minutes apart. Like the `>1 canonical` count, it is derived from a *paginated* scan of `releases` ([mb-qc.ts:50-61](apps/web/scripts/mb-qc.ts#L50-L61)), so concurrent writes shift rows between pages and change the total. Don't act on it; don't read a single value as signal. **Environmental issue worth knowing: long-lived Supabase clients wedge into permanent `TypeError: fetch failed`.** Confirmed in the pipeline's own output (`[qc] lane crashed: TypeError: fetch failed — restarting in 30s` on repeat, plus `gapfill`), and independently in two monitoring processes that died the same way after 40m–3h while brand-new processes connected instantly. It is a stale keep-alive socket, **not** a DB outage. Real consequence: the `newreleases` lane **did** run its second sweep at **03:54Z** (cursor file proves it) but could not write its heartbeat, so **the flagged count — the lane's first genuinely usable number — was lost**. Catalog: artists 67,774 · release_groups 479,427 · **covered 395,005** (cover backfill complete: 30,977 filled). **Nothing auto-fixed.** **Next due: 2026-08-25** (weekly). **✅ `newreleases` FIRST USABLE NUMBER CAPTURED (2026-08-20 04:11–04:15Z): `+428 flagged`, sweep took ~4 minutes, err=0.** This is the measurement lost twice before (08-18's `+27` was an artifact of the whole catalog being overdue; 08-19's was lost when the lane couldn't write its heartbeat during the `fetch failed` window). It went up because the **claim-on-read fix** now pushes claimed artists' `next_check_at` into the future, and only future-dated artists can be pulled forward by a sweep. **Do not compare 428 to the "~75 artists/day" design figure** — the window is 45 days back + 30 forward, not one day, and eligibility still excludes everything in the overdue backlog, so this remains a *partial* count that should grow as that backlog drains. The design claim it does validate: ~4 minutes of MB time surfaced 428 artists worth re-polling, against the ~2.3 days a full per-artist sweep would cost. **Second sweep 2026-08-21 04:14–04:20Z: `+521 flagged`** (~6 min, err=0, cumulative 949) — the predicted upward drift as the claim fix moves more artists to future `next_check_at` values and thus into eligibility. Expect it to keep climbing toward a steady state rather than settling at any of these early numbers. |
| 2026-08-18 (post-fix restart) | 5/7 (`integrity` known set · `tiering` timeout) | n/a — see the throughput note | 0 | **Verification check for the FRESHNESS livelock fix in the row below. ✅ THE FIX WORKS, confirmed live.** Within seconds of restart, Bob Dylan's `next_check_at` moved `2026-06-28` → **`2036-08-15`** (the heavily-featured park), and the worker moved on instead of re-claiming him. **2 artists parked** in total, then FRESHNESS started doing real work: **6 completed re-polls**, including **`Anuel AA +2 new groups`** — independently corroborated by a `release_groups` count that rose by exactly +2 in the same window. Contrast with the pre-fix state, where that counter sat at 0 for 90+ minutes. **⚠ Read the throughput line correctly here: `last 60m 0/hr` is NOT a stall.** That figure counts **queue drains only**, and under `--fresh-focus` the queue is *supposed* to be empty (`discover`/`area` off, nothing tops it up) — all the real work is FRESHNESS re-polls, which that metric does not count. Judge progress by `freshness` `items_done` and catalog counts instead. Likewise `ingest: idle` next to a *running* freshness lane is the correct steady state. **`newreleases` correctly did NOT re-sweep** — `idle «next sweep in ~21h»`, its persisted cursor honored across the restart, so the restart cost no MB time. **verify fails, both known:** `integrity` is the same set as the row below (**0-canonical is now 6, up from 4 earlier the same day and 1–2 in July — this is the third consecutive check where it has ticked up; it is still trivial in absolute terms but it is now a trend, so read it again next check**); `tiering` timed out, having *passed* earlier the same day — the difference is write load, since the catalog-wide Deezer cover backfill (~1 update/sec) was running concurrently. Taken together with 08-10 (timed out against an **idle** DB), `tiering` is simply borderline against a 4.6M-row `release_tracks` and will flip either way — **don't read a single tiering result as signal**. Also visible: `covered` climbed **372,218 → 372,948 (+730)** from that cover backfill. **Nothing auto-fixed.** **Next due: 2026-08-25** (weekly), **plus tomorrow's `newreleases` sweep** for the lane's first usable number. |
| 2026-08-18 | 6/7 (`integrity` only — all four sub-counts known/benign) | 48/hr on the 10m window ✅ (the 60m read of 8/hr is understated — 4 weeks of downtime sit in it) | 41 | **Post-restart check** — user restarted with `npm run pipeline:fresh` (`--fresh-focus --no-embed`) after ~4 weeks down (since ~07-19); this is the check 08-10 deferred to "immediately on pipeline restart". **Healthy.** Queue draining (48 → 41 pending, done 35,245 → 35,252) with **failed still 8, unchanged since 07-19** — no new failures on the restart. Catalog: artists 67,532 · release_groups 478,130 · releases 474,294 · recordings 3,563,911 · release_tracks 4,580,123. The 3.60M → 3.56M recordings step is **consistent with the 43,135 video rows deleted on 08-12**, not loss. **Lane states are all correct for `--fresh-focus`:** `area` off (`AREA_DISCOVERY=0`) and `discover` off (`--no-discover`) are the flag doing its job, not lanes failing; `embeddings` off with a stale `err=500` is the documented `--no-embed` carryover. **`reconcile`, `gapfill`, and `qc` heartbeats all came back** (they were 30–32 days stale at the 08-10 read) and reconcile linked 1. **`tiering` now PASSES** (ran, 0 changed) — it was the second failure on 08-10, and notably it timed out *there* against an idle DB but succeeded *here* under live ingest load, so that timeout was query cost that happened to fall the other way, not a load problem. **The one fail, `integrity`, is four known sub-counts:** (1) **1,417 duplicate artist names — flat vs 08-10's 1,417**, so the 635 → 1,417 July→August climb flagged there has *stopped*; (2) **132,477 >1-canonical**, the known paging-race false positive (it read 131,277 and 123,055 in other calls *in the same session* — that fluctuation is the paging race's own signature); (3) **4 groups with 0 canonical**, up from 1–2 (07-10: 1 · 07-12: 2 · 08-10: 1 · this session: 2 → 4). Still trivial in absolute terms but it has now ticked up three checks running — **re-read this one next check**; (4) **336 unexpected-source rows — this is NOT a drop from 08-10's 834.** 336 = exactly 168 release_groups + 168 releases; the `recordings` term contributed **0 because `recordings` `count(*)` was timing out under the restart's write load** (the same run's `pipeline:status` printed `recordings 0`). Verified read-only rather than assumed: `source='spotify'` rows are still present in **all three** tables, and a direct count returned **3,563,782 recordings in 5.8s** once load eased. Same stale-allowlist non-issue as 08-10 — `mb-qc.ts`'s allowlist is `musicbrainz\|itunes\|deezer` and predates `scripts/resolve-empty-artists.ts`. qc's `tracks_done` artists with 0 RGs is **down 14,530 → 10,062**. **`newreleases` swept for the first time ever and completed cleanly in ~21 min** (`+27 flagged; next in ~24h`, err=0). The lane beats once at sweep start and once at completion by design, so a heartbeat sitting at `«sweeping MB…»` for minutes is expected rather than a hang, and the watchdog is ingest-only so it can't restart-loop a long sweep. **⚠ Do not read `flagged=27` as "only 27 of our artists have new releases" — that is not what the number counts.** The flag update is `.gt('next_check_at', dueAt)` ([discover-mb-newreleases.ts:117](apps/web/scripts/discover-mb-newreleases.ts#L117)), i.e. it only ever moves `next_check_at` **backward**, deliberately, so a repeat sweep can't shuffle the queue or starve a long-overdue artist. After 4 weeks down **essentially the whole catalog was already overdue**, so almost every genuine match was skipped as "already due" and never counted. 27 is the size of the *not-already-due* remainder, an artifact of the backlog. **The first meaningful measurement of this lane is the NEXT daily sweep**, once FRESHNESS has worked through the backlog and `next_check_at` values are in the future again — compare that number against the ~75/day the design predicts. The genuinely informative counts (groups swept, distinct credited artists, how many are ours, `unowned` expansion candidates) are console-only and were not captured this run. **Queue fully drained during the check** (41 → **0 pending**, done 35,294); `ingest: idle` is the queue-empty label, **not** a stall — FRESHNESS takes the shared MB worker "whenever the queue is empty", which is why the catalog kept growing with `pending 0` (+86 artists / +586 release_groups / ~5,500 recordings per hour). That is the correct steady state for `--fresh-focus`, where `discover` is off and nothing tops the queue up. **🐛 THE REAL FINDING OF THIS CHECK — the pipeline passed every lane check and was still collecting nothing.** ~20 min after the queue drained, the catalog went completely flat (last ingest 03:42 "Urbanus"; `next_check_at`, `last_ingested_at` and the `search_misses` backlog all frozen for 90+ min) while `pipeline:status` kept reporting healthy lanes. **Root cause: a FRESHNESS livelock.** FRESHNESS claims the head of the hot tier (Bob Dylan), `ingestArtist` throws `HeavilyFeaturedError` (> `MAX_INGEST_RGS` = 800, the Mozart/Bach class), and the catch logged and returned `true` **without rescheduling `next_check_at`** — so the next pass re-claimed the same artist and failed identically, forever. **It was silent because the lane's heartbeat sits at the END of `refreshArtist`**, so the lane never beat and never reported an error: a stale `idle` freshness lane plus an `idle` ingest lane looks exactly like a healthy drained queue. The **queue** path has guarded this since 07-01 ("skip terminally, don't fail+requeue into a loop"); the **freshness** path never got the same guard, and the **2026-08-10 hot-tier-first ordering change made it reachable immediately**, because the >800-RG artists are concentrated in the hot tier. Diagnosis was DB-only (the ~10–20s loop period matches paging 800+ RGs at MB's 1 req/s; two earlier hypotheses — a due artist with no MB external id, and a `tryMisses` loop on the 68 unqueued `search_misses` — were both tested and refuted). **Fixed in code** ([pipeline.ts](apps/web/scripts/pipeline.ts) `tryFreshness`): the catch now always advances the schedule — heavily-featured parks `FRESHNESS_HEAVY_SKIP_DAYS` (~10y, the freshness equivalent of the queue's terminal `skipped`; deliberately not `null`, since `bootstrapFreshness()` re-schedules nulls at startup and would re-enter the loop), anything else backs off `FRESHNESS_ERROR_BACKOFF_DAYS` (1d, transient-friendly). `tsc --noEmit` clean. **No data fix needed — it self-heals on restart**: each heavily-featured artist now costs one failed attempt (~15s) and is then parked. **Lesson for this file: `pipeline:status` lane health is NOT proof of progress.** Check that `last_ingested_at` / catalog counts are actually moving. **Nothing auto-fixed.** **Next due: immediately on the next pipeline restart** (verify FRESHNESS actually re-polls — watch for `[freshness] skipped (heavily featured) …` lines followed by real re-polls), then **2026-08-25** weekly — **plus a one-off look at tomorrow's `newreleases` sweep** for the number above. |
| 2026-08-10 | 5/7 (1 stale-allowlist false positive · 1 timeout) | 0/hr (pipeline STOPPED ~21 days — every lane heartbeat ~1.85M s old) | 46 | **Pre-restart check** (user asked whether to restart the pipeline after ~3 weeks off; ran both checks *while the DB was idle*, which is the one time `verify` doesn't contend with ingest write load). Queue: 46 pending · 4 processing (stale, `resetStale` re-queues on restart) · 8 failed (unchanged since 07-19) · 35,244 done. Catalog grew slightly while stopped (478,069 RGs / 67,522 artists / 3.60M recordings) — from the out-of-pipeline empty-artist resolver, not ingest. **verify fails: (1) `integrity`** = 1,417 dup artist names (the known same-name-distinct class, but **up from 635 in July** — worth a look if it keeps climbing), 124,465 >1-canonical (**known paging-race false positive**), and **834 "unexpected-source" rows — NOT a real anomaly:** they're `source='spotify'` (168 release_groups + 168 releases + ~498 recordings, all created 2026-07-31 03:27–03:40 UTC) written by **`scripts/resolve-empty-artists.ts`**, the documented zero-release-artist recovery lane. `mb-qc.ts`'s source allowlist is `musicbrainz|itunes|deezer` and simply **predates the Spotify resolver** — the allowlist is stale, the data is fine. **(2) `tiering`** = `canceling statement due to statement timeout`, notable because there was *no* write load this time (so it's query cost against a 4.6M-row `release_tracks`, not contention) — that check went unverified. qc also reports 14,530 tracks_done artists with 0 RGs (the documented MB coverage gap; **up from 8,973 on 07-28**). **Nothing auto-fixed.** **Next due: immediately on pipeline restart** (re-run both, and expect the `area`/`recency`/`reconcile` lane heartbeats to reappear). |
| 2026-07-19 | not run (both pipeline + backfill sweep STOPPED by user after a ~2-day run — no live process to check against) | n/a (stopped) | 46 | **Session-close final check.** The `discover:area` KR discovery batch fully drained: **KR artists ~1,293 → 12,413** (~complete vs MB's ~12,533), total artists 67,518, `release_groups` 474,940 (**513 `source='itunes'`** from the backfill/recency lanes). Queue: 46 pending · 35,244 done · **8 failed (unchanged all run)** · 2,649 skipped · 4 processing (stale — mid-flight when stopped, `resetStale` re-queues on restart). No new failures across the whole 2-day drain. New this session: `area` pipeline lane (activates on restart), iTunes `backfill` lane (one-off), `resolve-thin-artists.ts` (dry-run only). **Next due: on next pipeline restart** — run `status`/`verify` then, and note the new `area` lane heartbeat is expected. |
| 2026-07-15 | not completed (stalled after the `migrations` check under live ingest write-load — the documented verify-vs-ingest contention; user Ctrl+C'd) | 30/hr ✅ (last-10m; the 60m read of 5/hr is understated — includes the restart + Félix-stall downtime) | 244 | **Post-restart check** (user restarted the pipeline `--no-embed` + `DISCOVER_CEILING=50000` after ~2d idle). **Restart incident:** ingest restart-looped on ListenBrainz artist **Félix Leclerc** (68 RGs) — the **ingest watchdog** (idle>~600s / no-MB-activity) forced repeated restarts, each reset re-serving him; set his queue row `status='skipped'` by hand (SQL editor) and he then **completed on a retry** (61 groups / 1502 recordings) → transient MB unresponsiveness, not a permanent poison pill, data landed. **Now draining healthily:** ingest `done=5` (on "Loverboy"), recordings ~618/hr, +8 artists (49443→49451). `status` catalog `release_groups`/`enrichment` read **0** = the known `count(*)`-timeout under write load, NOT data loss (`recordings` 3.178M and `releases`/`release_tracks` return real climbing numbers). qc warnings the known-benign set (635 same-name dups, 113,979 >1-canonical paging-race, 1 zero-canonical, 4,505 tracks-done/0-RG). **Also this session:** ran the new **credit-graph discovery lane** (`discover:credits`) live → **queued 86** `source='mbid'` feat-only KR artists (11 deduped already-owned, 0 dupes), sitting behind the ~164 ListenBrainz batch. **Watch:** the watchdog restarts on a hang but doesn't auto-skip repeat offenders (`attempt_count` stayed 1) — future poison pills will loop until manually skipped unless an attempt-based auto-skip is added (proposed, not built). **Next due: 2026-07-22** (weekly) or on restart / if ingest re-stalls on a single artist. |
| 2026-07-12 | not run (live ingest running — verify only adds contention per 07-10 precedent; migration `20260712000009` (taste_profiles, renumbered at merge) touches ratings/taste tables only, no pipeline write path) | 63/hr ✅ | 28 | **Post-migration check** (taste_profiles: `genre_vectors` + `user_taste_profiles` + ratings trigger — applied + verified functionally incl. trigger insert/update/delete round-trip). Pipeline healthy: ingest beat 29s (Kaydy Cain), gapfill running (covers 289), discover topping up, queue nearly drained (28 pending, failed 8). `status` catalog counts show the **known count(*)-timeout symptom** (release_groups/recordings read 0 under write load) — NOT data loss; a direct query this session returned 400,925 RGs in ~4s. qc warnings are the known-benign set (610 same-name dups, >1-canonical paging race — now 102,799 with catalog growth, 1,921 tracks-done/0-RG) **plus 0-canonical now 2 (was 1 on 07-10)** — still trivial but trending, worth a look if it keeps growing. embeddings lane off with stale err=500 carryover (by design). **Next due: 2026-07-17** (weekly) or on restart. |
| 2026-07-10 | not completed (timed out at 180s on `integrity` under live ingest write-load — the documented verify-vs-ingest contention; `migrations` check passed) | 49/hr ✅ | 129 | **Overnight-run health check** (user restarted the pipeline in their own terminal with `--no-embed` + `DISCOVER_CEILING=50000`). **Healthy.** Catalog grew overnight: artists 33,991→**34,213** (+222), release_groups 294,797→**297,697** (+2,900), recordings +23k, covers 232,708→**235,006**. All active lanes running, err=0, fresh heartbeats (ingest on 矢井田瞳); **no IO starvation** — counts return real numbers (not the `0`s of the 07-07 incident). DISCOVER ceiling raised worked → 14,456/50,000, actively queuing KR/JP. `qc` structural warnings all **known-benign**: 373 same-name-distinct dups (김광석-class), 78,362 >1-canonical (paging-race false positive), 1,143 tracks_done/0-RG (features-only) — PLUS **1 group with 0 canonical** (new but trivial — 1 group, likely a mid-ingest transient; re-check next pass). embeddings lane off by design (`--no-embed`); its err=500 is stale carryover. Didn't re-run verify to completion (would only add contention to the live ingest); trust `qc` + `status` here. **Next due: 2026-07-17** (weekly steady-state) or on next restart. |
| 2026-07-07 | not run (DB IO-starved; verify would only add load) | 0/hr (pipeline off, heartbeats ~2.3d stale) | 0 | **Post-migration check** (applied `20260706000016` silla-precomputed-restore + `20260706000017` search-trgm via new `scripts/db-exec.ts` Management-API path — both verified functionally). `pipeline:status` catalog counts read **0** (release_groups/recordings/enrichment) — its count(*) queries are timing out, NOT data loss (plain queries return rows; yesterday's real counts: 295k RGs / 2.3M recordings). Cause: an **out-of-repo PostgREST client bulk-updating `release_groups.primary_genre`** (no migration defines that column; likely a Mac-side script) at ~1 row/3.8s × 4 workers, 279,646 rows remaining — IO-starves the Micro. Don't trust status/verify output until it's stopped; then re-run both. **Next due: 2026-07-13**, or immediately after the backfill stops / next restart. |
| 2026-07-06 | not run (pipeline not running) | 0/hr (stale) | 0 | **Pipeline is NOT running** — all lane heartbeats ~39h old (machine was off/restarted); queue fully drained (done 12,797 · skipped 1,564 · failed 5). Catalog: artists 33,991 · release_groups 294,797 · recordings 2.3M · covers 232k/295k · embedded 242k/295k. Carry-over flags unchanged from 07-05: embeddings lane last errored with statement timeouts (err=500) before it stopped; qc's 77k multi-canonical warning is the known paging-race false positive. Since the 5k drain is complete, cadence moves to **weekly** per Schedule — but re-run `pipeline:status`/`pipeline:verify` immediately after the next pipeline restart (and retry `reconcile_prestige_scores(5000)` then, per the 07-05 note). **Next due: 2026-07-13** or on restart. |
| 2026-07-05 | not run (bug-driven check, not scheduled) | 1/hr | 0 | **get_silla_leaderboard fixed (iOS "랭킹" card showed "no data").** Root cause: not a data gap — the RPC was hitting the anon role's statement timeout against ~290k release_groups, silently swallowed to an empty array by the client's `try?`. Took 4 rounds to land a durable fix (`20260704000000` → `20260705000000/1/2`): naive small→large join restructuring wasn't enough (planner kept picking a bad plan — 1,823 individual random-access lookups, then a full 294k-row ordered index scan when nested-loop was disabled, then merge-join breaking the country-scoped path when hash-join was forced globally). Durable fix: global path now reads `release_groups.prestige_score` (precomputed, already indexed) directly — no join at all; country path keeps live computation with just `enable_nestloop=off`. Verified stable: 359-913ms across repeated calls, both paths, no timeouts. Also fixed `reconcile_prestige_scores()` (an ad hoc SQL-editor function, never in a tracked migration) to use the current prestige formula (v2 — the old one predates the per-tier-max/floor-guarantee fix) — `20260705000003`. **Left un-run:** the actual data refresh (`reconcile_prestige_scores(5000)`, ~234 stale-formula rows out of 1,589) — it collided with the `embeddings` lane, which is itself actively erroring against `release_groups` (`err=474 «canceling statement due to statement timeout»` per `pipeline:status`). This is the same recurring category of issue as 2026-06-29/07-01's reconcile write-load contention — not a new bug. Retry the reconcile once the embeddings lane's error rate settles. **Next due: 2026-07-06**, or immediately if retrying the reconcile. |
| 2026-07-01 | 5/7 (both fails known) | 102/hr ✅ | 9318 | **Data-fix + expansion session drain check.** Catalog grew hugely: artists 7.2k→**10,993**, release_groups 73k→**114,879** (+42k), recordings 786k — the credit-stub (4.6k), popularity (Last.fm famous-first), KR-scene, and prestige-canon (`seed:missing-from-external` 611 albums) queues are ingesting. **0 failed**; heavily-featured hard-skip (>800 RGs) live → **Mozart clog fixed** (was restart-looping; skipped Mozart+Bach/Beethoven/Chopin/Liszt/Brahms/Wagner/Debussy). verify fails: (1) **integrity** = 90 benign same-name dups (grew with catalog) + 1 orphan release (web-search stray); (2) **charts-rpcs** `get_charts_top_rated_songs` **times out (57014)** at 786k recordings under ingest load — song charts degrade via `try?`; needs the reconcile treatment (index/timeout/batch). **reconcile FIXED this session** — batched (`batch_limit`, only-changed rows) after a 4-layer debug (statement-timeout→lock→zombie txn→HNSW/GIN index churn); migrations `20260701000001/2/3`. Prestige overrides: 33 hand-mapped classics + `backfill-external-mbids` (resolve→browse, CJK-aware). ETA ~3.8d. **Next due: 2026-07-02** |
| 2026-06-29 (late) | 6/7 (3 benign same-name dups) | n/a (prestige-prioritized) | ~1990 | **Prestige Western-ingest + padDate fix.** 726 modern Western prestige artists queued at priority → catalog **16k→~71k release_groups** (canon comp-heavy: Beatles 1,730 releases, etc.). Found+fixed `padDate` `??` partial-date crash (`"1994-??-11"` → aborted ingest for Mariah Carey/Luther Vandross/Damon Albarn) — committed `2928721`, verified live after restart (Luther re-ingested 71 RGs clean). Cleanliness audit: source-pure (70.6k MB/358 deezer/0 itunes/null), blocklist holding (0 VA leaks), 0 broken FKs; cleaned 1 orphan ("Salad Days"). verify's 3 dup-name flags benign (김광석 singer/drummer · S.E.S. group/jungle · Cream supergroup/Polish-DJ, each partner 0 RGs). `reconcile_prestige_scores` now **times out (57014)** under the ingest write-load — re-run after prestige queue drains. **Next due: 2026-06-30** |
| 2026-06-29 | 6/7 (same 김광석 false-positive) | post-restart (understated) | ~3369 | **"Various Artists" mega-match incident.** "Ray" mis-resolved to MB Various Artists (324k releases) → worker stuck ~71 min crawling + polluting (75 RGs/1143 recordings written). Fixed: **special-MBID blocklist** (`SPECIAL_MBIDS` in mb-ingest, filters resolver + guards ingest); VA footprint purged FK-safe w/ shared-recording check (all VA-exclusive). Pipeline restarted → blocklist + watchdog now live; drain resumed; "Ray"→needs_review. Watchdog wouldn't catch this (MB calls were flowing = "alive but slow") — distinct from a hang. **Next due: 2026-06-30** |
| 2026-06-28 (pm) | 6/7 (same 김광석 false-positive) | post-restart (understated) | 3520 | **Resolver fix verified live + a silent hang caught.** Overnight drain good (4919→~3522). Found the MB **ingest worker hung ~15 min** (frozen after `Dialogue → DIALOGUE+`, no error thrown — an untimed `await`; DISCOVER/EMBEDDINGS kept beating so not a sleep). Supervisor only restarts on *throw*, so a hang escapes it → **needs a heartbeat watchdog** (proposed). User restarted; draining resumed (pending ticking down, ingest beat fresh). Skip spike 52→909 is **benign**: 885 `no_match` (DISCOVER snowball churn) + only 9 from the new short-CJK guard. The 7 re-queued shadows resolved as designed: 리제→done(LeeZe), other 6→needs_review. **Next due: 2026-06-29** |
| 2026-06-28 (am) | 6/7 (integrity flag is a **false positive**) | 84/hr ✅ | 4919 | Throughput recovered hard: 84/hr (10m window ~258/hr) → ETA ~2.4d, comfortably inside the 2-wk window (local-mirror plan **not** needed). verify's lone fail = "1 duplicate artist name" = **김광석 ×2**, but they're two distinct MB artists (`singer` vs `drummer`, different MBIDs/disambiguation) — name-only check can't tell them apart; **benign, no merge**. mb:audit canonical integrity clean (>1 canonical=0, 0 canonical=0). report_block tables (`reports`/`blocked_users`) confirmed EXIST in prod (README migration table stale). **Next due: 2026-06-29** |
| 2026-06-27 | 7/7, structurally clean (5 empty, benign) | 20/hr | 5091 | Web orphan leak fixed at root (search-insert) + **deployed** (fd66fea) + 20 orphans cleaned — won't recur. Deezer fallback lane live (`DEEZER_FALLBACK=1`, idle). Rate ~20/hr → ETA ~10.6d (within the 2-wk window but **tight** — keep watching; if it holds ≤20/hr, stand up the local MB mirror). **Next due: 2026-06-28** |
| 2026-06-26 | 7/7, structurally clean (4 empty, benign) | 18/hr ⚠ | 5178 | iTunes-shadow cleanup done + job C disabled. Rate dipped (restart/cleanup downtime in window + prolific stretch) — re-check 2026-06-27; if ≤20/hr persists, consider local mirror. qc lane heartbeat stale (pre-fix code) — clears on next restart. |
