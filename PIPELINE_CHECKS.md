# Pipeline health checks

Periodic health checks for the MusicBrainz catalog pipeline.

> **If you are a Claude Code session, do this at session start:** read **Schedule** + the
> top row of **Check log** below. If today's date ≥ `next due` **and** the pipeline is
> running, then RUN the commands in **How to run**, interpret per **Reading the results**,
> report a short summary to the user, and **prepend a new dated row to the Check log** with
> the next due date. If it's not due yet, do nothing (don't nag). Always run the checks
> (regardless of due date) right after a pipeline restart, a migration, or a pipeline code
> change. Convert any relative dates to absolute. Don't auto-fix data — report and propose.

## Schedule (cadence)
- **Daily** while the launch 5k is draining — until `pending` ≈ 0 (est. **~2026-07-03 to 2026-07-08**).
- **Weekly** afterward (steady state: FRESHNESS re-polls + slow DISCOVER growth).
- **Always** after: a pipeline restart · a migration applied · a pipeline code change · the machine was off for hours.

## How to run (from `apps/web/`)
```
npm run pipeline:status   # liveness, throughput, ETA  (daily)
npm run pipeline:verify   # 7-check structural battery  (daily)
npm run mb:audit          # deeper data view: sample eyeball, skipped list, source purity, ISRC  (every ~3 days, or if verify flags)
```

## Reading the results — what's fine vs a real problem
**GOOD (no action):**
- `pipeline:verify` → **7/7, "structurally clean"**. The `N empty artist(s) to review` note is **benign** — features-only acts (e.g. SEHUN) legitimately have 0 core releases after the composition filter, plus a few known generic-name false-matches (kai/dean/gray class, 0 release_groups = no bad data).
- `pipeline:status` throughput: healthy is **≳ 25 artists/hr**. A reading taken right after a restart is **understated** (downtime sits in the 60-min window) — re-check before reacting.
- The `qc` **lane** heartbeat in `pipeline:status` can lag the live truth (it re-runs hourly and reflects the running process's code). **Trust `pipeline:verify` over a stale qc heartbeat.**

**REAL problems → tell the user / investigate:**
- `pipeline:status` shows `last 60m 0/hr` while `ingest` says `running` → **stalled** (machine asleep? MB unreachable? supervisor stuck).
- `pipeline:verify` **structural** fail — duplicate artists, canonical (>1 / 0 canonical), orphan releases, or unexpected `source` (NOT the empty-artists note).
- `pending` not decreasing across a full day.
- `mb:audit` sample shows artists with a country that contradicts the seed region (e.g. a Korean name as `[US]`/`[AU]`) → false-match (the iTunes-shadow / generic-name class).
- Throughput holding **≤ 20/hr** for multiple days → 5k won't finish in the 2-week window; revisit the local-MB-mirror option.

## Check log (newest first)
| Date | verify | rate (last 60m) | pending | notes / next due |
|------|--------|-----------------|---------|------------------|
| 2026-07-12 | not run (live ingest running — verify only adds contention per 07-10 precedent; migration `20260712000009` (taste_profiles, renumbered at merge) touches ratings/taste tables only, no pipeline write path) | 63/hr ✅ | 28 | **Post-migration check** (taste_profiles: `genre_vectors` + `user_taste_profiles` + ratings trigger — applied + verified functionally incl. trigger insert/update/delete round-trip). Pipeline healthy: ingest beat 29s (Kaydy Cain), gapfill running (covers 289), discover topping up, queue nearly drained (28 pending, failed 8). `status` catalog counts show the **known count(*)-timeout symptom** (release_groups/recordings read 0 under write load) — NOT data loss; a direct query this session returned 400,925 RGs in ~4s. qc warnings are the known-benign set (610 same-name dups, >1-canonical paging race — now 102,799 with catalog growth, 1,921 tracks-done/0-RG) **plus 0-canonical now 2 (was 1 on 07-10)** — still trivial but trending, worth a look if it keeps growing. embeddings lane off with stale err=500 carryover (by design). **Next due: 2026-07-17** (weekly) or on restart. |
| 2026-07-10 | not completed (timed out at 180s on `integrity` under live ingest write-load — the documented verify-vs-ingest contention; `migrations` check passed) | 49/hr ✅ | 129 | **Overnight-run health check** (user restarted the pipeline in their own terminal with `--no-embed` + `DISCOVER_CEILING=50000`). **Healthy.** Catalog grew overnight: artists 33,991→**34,213** (+222), release_groups 294,797→**297,697** (+2,900), recordings +23k, covers 232,708→**235,006**. All active lanes running, err=0, fresh heartbeats (ingest on 矢井田瞳); **no IO starvation** — counts return real numbers (not the `0`s of the 07-07 incident). DISCOVER ceiling raised worked → 14,456/50,000, actively queuing KR/JP. `qc` structural warnings all **known-benign**: 373 same-name-distinct dups (김광석-class), 78,362 >1-canonical (paging-race false positive), 1,143 tracks_done/0-RG (features-only) — PLUS **1 group with 0 canonical** (new but trivial — 1 group, likely a mid-ingest transient; re-check next pass). embeddings lane off by design (`--no-embed`); its err=500 is stale carryover. Didn't re-run verify to completion (would only add contention to the live ingest); trust `qc` + `status` here. **Next due: 2026-07-17** (weekly steady-state) or on next restart. |
| 2026-07-07 | not run (DB IO-starved; verify would only add load) | 0/hr (pipeline off, heartbeats ~2.3d stale) | 0 | **Post-migration check** (applied `20260706000016` silla-precomputed-restore + `20260706000017` search-trgm via new `scripts/db-exec.ts` Management-API path — both verified functionally). `pipeline:status` catalog counts read **0** (release_groups/recordings/enrichment) — its count(*) queries are timing out, NOT data loss (plain queries return rows; yesterday's real counts: 295k RGs / 2.3M recordings). Cause: an **out-of-repo PostgREST client bulk-updating `release_groups.primary_genre`** (no migration defines that column; likely a Mac-side script) at ~1 row/3.8s × 4 workers, 279,646 rows remaining — IO-starves the Micro. Don't trust status/verify output until it's stopped; then re-run both. **Next due: 2026-07-13**, or immediately after the backfill stops / next restart. |
| 2026-07-06 | not run (pipeline not running) | 0/hr (stale) | 0 | **Pipeline is NOT running** — all lane heartbeats ~39h old (machine was off/restarted); queue fully drained (done 12,797 · skipped 1,564 · failed 5). Catalog: artists 33,991 · release_groups 294,797 · recordings 2.3M · covers 232k/295k · embedded 242k/295k. Carry-over flags unchanged from 07-05: embeddings lane last errored with statement timeouts (err=500) before it stopped; qc's 77k multi-canonical warning is the known paging-race false positive. Since the 5k drain is complete, cadence moves to **weekly** per Schedule — but re-run `pipeline:status`/`pipeline:verify` immediately after the next pipeline restart (and retry `reconcile_prestige_scores(5000)` then, per the 07-05 note). **Next due: 2026-07-13** or on restart. |
| 2026-07-05 | not run (bug-driven check, not scheduled) | 1/hr | 0 | **get_silla_leaderboard fixed (iOS "랭킹" card showed "no data").** Root cause: not a data gap — the RPC was hitting the anon role's statement timeout against ~290k release_groups, silently swallowed to an empty array by the client's `try?`. Took 4 rounds to land a durable fix (`20260704000000` → `20260705000000/1/2`): naive small→large join restructuring wasn't enough (planner kept picking a bad plan — 1,823 individual random-access lookups, then a full 294k-row ordered index scan when nested-loop was disabled, then merge-join breaking the country-scoped path when hash-join was forced globally). Durable fix: global path now reads `release_groups.prestige_score` (precomputed, already indexed) directly — no join at all; country path keeps live computation with just `enable_nestloop=off`. Verified stable: 359-913ms across repeated calls, both paths, no timeouts. Also fixed `reconcile_prestige_scores()` (an ad hoc SQL-editor function, never in a tracked migration) to use the current prestige formula (v2 — the old one predates the per-tier-max/floor-guarantee fix) — `20260705000003`. **Left un-run:** the actual data refresh (`reconcile_prestige_scores(5000)`, ~234 stale-formula rows out of 1,589) — it collided with the `embeddings` lane, which is itself actively erroring against `release_groups` (`err=474 «canceling statement due to statement timeout»` per `pipeline:status`). This is the same recurring category of issue as 2026-06-29/07-01's reconcile write-load contention — not a new bug. Retry the reconcile once the embeddings lane's error rate settles. **Next due: 2026-07-06**, or immediately if retrying the reconcile. |
| 2026-07-01 | 5/7 (both fails known) | 102/hr ✅ | 9318 | **Data-fix + expansion session drain check.** Catalog grew hugely: artists 7.2k→**10,993**, release_groups 73k→**114,879** (+42k), recordings 786k — the credit-stub (4.6k), popularity (Last.fm famous-first), KR-scene, and prestige-canon (`seed:missing-from-external` 611 albums) queues are ingesting. **0 failed**; heavily-featured hard-skip (>800 RGs) live → **Mozart clog fixed** (was restart-looping; skipped Mozart+Bach/Beethoven/Chopin/Liszt/Brahms/Wagner/Debussy). verify fails: (1) **integrity** = 90 benign same-name dups (grew with catalog) + 1 orphan release (web-search stray); (2) **charts-rpcs** `get_charts_top_rated_songs` **times out (57014)** at 786k recordings under ingest load — song charts degrade via `try?`; needs the reconcile treatment (index/timeout/batch). **reconcile FIXED this session** — batched (`batch_limit`, only-changed rows) after a 4-layer debug (statement-timeout→lock→zombie txn→HNSW/GIN index churn); migrations `20260701000001/2/3`. Prestige overrides: 33 hand-mapped classics + `backfill-external-mbids` (resolve→browse, CJK-aware). ETA ~3.8d. **Next due: 2026-07-02** |
| 2026-06-29 (late) | 6/7 (3 benign same-name dups) | n/a (prestige-prioritized) | ~1990 | **Prestige Western-ingest + padDate fix.** 726 modern Western prestige artists queued at priority → catalog **16k→~71k release_groups** (canon comp-heavy: Beatles 1,730 releases, etc.). Found+fixed `padDate` `??` partial-date crash (`"1994-??-11"` → aborted ingest for Mariah Carey/Luther Vandross/Damon Albarn) — committed `2928721`, verified live after restart (Luther re-ingested 71 RGs clean). Cleanliness audit: source-pure (70.6k MB/358 deezer/0 itunes/null), blocklist holding (0 VA leaks), 0 broken FKs; cleaned 1 orphan ("Salad Days"). verify's 3 dup-name flags benign (김광석 singer/drummer · S.E.S. group/jungle · Cream supergroup/Polish-DJ, each partner 0 RGs). `reconcile_prestige_scores` now **times out (57014)** under the ingest write-load — re-run after prestige queue drains. **Next due: 2026-06-30** |
| 2026-06-29 | 6/7 (same 김광석 false-positive) | post-restart (understated) | ~3369 | **"Various Artists" mega-match incident.** "Ray" mis-resolved to MB Various Artists (324k releases) → worker stuck ~71 min crawling + polluting (75 RGs/1143 recordings written). Fixed: **special-MBID blocklist** (`SPECIAL_MBIDS` in mb-ingest, filters resolver + guards ingest); VA footprint purged FK-safe w/ shared-recording check (all VA-exclusive). Pipeline restarted → blocklist + watchdog now live; drain resumed; "Ray"→needs_review. Watchdog wouldn't catch this (MB calls were flowing = "alive but slow") — distinct from a hang. **Next due: 2026-06-30** |
| 2026-06-28 (pm) | 6/7 (same 김광석 false-positive) | post-restart (understated) | 3520 | **Resolver fix verified live + a silent hang caught.** Overnight drain good (4919→~3522). Found the MB **ingest worker hung ~15 min** (frozen after `Dialogue → DIALOGUE+`, no error thrown — an untimed `await`; DISCOVER/EMBEDDINGS kept beating so not a sleep). Supervisor only restarts on *throw*, so a hang escapes it → **needs a heartbeat watchdog** (proposed). User restarted; draining resumed (pending ticking down, ingest beat fresh). Skip spike 52→909 is **benign**: 885 `no_match` (DISCOVER snowball churn) + only 9 from the new short-CJK guard. The 7 re-queued shadows resolved as designed: 리제→done(LeeZe), other 6→needs_review. **Next due: 2026-06-29** |
| 2026-06-28 (am) | 6/7 (integrity flag is a **false positive**) | 84/hr ✅ | 4919 | Throughput recovered hard: 84/hr (10m window ~258/hr) → ETA ~2.4d, comfortably inside the 2-wk window (local-mirror plan **not** needed). verify's lone fail = "1 duplicate artist name" = **김광석 ×2**, but they're two distinct MB artists (`singer` vs `drummer`, different MBIDs/disambiguation) — name-only check can't tell them apart; **benign, no merge**. mb:audit canonical integrity clean (>1 canonical=0, 0 canonical=0). report_block tables (`reports`/`blocked_users`) confirmed EXIST in prod (README migration table stale). **Next due: 2026-06-29** |
| 2026-06-27 | 7/7, structurally clean (5 empty, benign) | 20/hr | 5091 | Web orphan leak fixed at root (search-insert) + **deployed** (fd66fea) + 20 orphans cleaned — won't recur. Deezer fallback lane live (`DEEZER_FALLBACK=1`, idle). Rate ~20/hr → ETA ~10.6d (within the 2-wk window but **tight** — keep watching; if it holds ≤20/hr, stand up the local MB mirror). **Next due: 2026-06-28** |
| 2026-06-26 | 7/7, structurally clean (4 empty, benign) | 18/hr ⚠ | 5178 | iTunes-shadow cleanup done + job C disabled. Rate dipped (restart/cleanup downtime in window + prolific stretch) — re-check 2026-06-27; if ≤20/hr persists, consider local mirror. qc lane heartbeat stale (pre-fix code) — clears on next restart. |
